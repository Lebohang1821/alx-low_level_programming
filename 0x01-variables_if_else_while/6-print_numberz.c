#include <stdio.h>

/**
 * main - prints the lowercase alphabet in reverse
 * followed by new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
